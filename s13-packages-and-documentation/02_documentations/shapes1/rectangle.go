package shapes1

// Area calculates and returns are of a rectangle
func Area(l, w int) int {
	return l * w
}

// Perim calculates and return perimeter of a reactangle
func Perim(l, w int) int {
	return 2 * (l + w)
}
